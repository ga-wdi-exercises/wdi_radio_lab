var config = {
  apiKey: "<your-api-key-here>",
  // authDomain: "<your-app-domain-here>",
  databaseURL: "<your-database-url-here>",
};
var APP = firebase.initializeApp(config);
var DB = APP.database();
});
