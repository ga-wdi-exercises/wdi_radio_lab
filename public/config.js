// Developer TODO: update api key and database url with personal copies from the firebase console
var config = {
  apiKey: '<Your-API-Key-Here>',
  databaseURL: "<Your-Database-Url-Here>",
};
firebase.initializeApp(config);
